% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/redcap.R
\name{redcap_resolve_cues}
\alias{redcap_resolve_cues}
\title{Resolve target cue settings for REDCap targets}
\usage{
redcap_resolve_cues(mode, name_meta, instrument_names, con)
}
\arguments{
\item{mode}{A character string indicating cue mode. One of:
\itemize{
\item \code{"logging"}: use REDCap project logs to detect changes.
\item \code{"always"}: always re-fetch data regardless of changes.
\item \code{"thorough"}: use default \code{targets} metadata-based invalidation.
\item \code{"never"}: never re-run unless missing or failed.
}}

\item{name_meta}{Name of the metadata target (used to check last run time).}

\item{instrument_names}{A character vector of instrument target names.}

\item{con}{A \code{redcapAPI::redcapConnection} object.}
}
\value{
A named list with \code{meta} and \code{inst} cue objects to use in
\code{\link[=tar_target]{tar_target()}} or \code{\link[tarchetypes:tar_map]{tarchetypes::tar_map()}}.
}
\description{
Determines how targets should be invalidated (\code{re-run}) based on the
specified mode. Used internally by \code{\link[=tar_redcap]{tar_redcap()}} to assign cue behavior
for both the metadata target and instrument-level data targets.
}
\keyword{internal}
